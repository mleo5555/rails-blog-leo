<h1>Latest Posts</h1>
  <% @posts.each do |post| %>
    <h3><%= link_to post.title, post_path(post) %></h3>
    <p>karma <%= post.karma %></p>
    <%= link_to 'Comments', post_path(post) %>
    <br>
  <% end %>
